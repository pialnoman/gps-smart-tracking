1. install all the required library one by one or by running the requirements.txt file
~python requirements.txt
or
~python3 requirements.txt

2. change the DB as needed for localhost or the live DB host (e.g. bluehost)
credentials; user: root, password: DM@bd!987

3. 